export const macroLocators = {
    macroButton: 'android=new UiSelector().text("Macro")',
    triggerButton: 'android=new UiSelector().text("Trigger")',
    applicationOption: 'android=new UiSelector().text("Application")',
    appInstallRemoveUpdate: 'android=new UiSelector().text("App Install/Remove/Update")',
    applicationRemoved: 'android=new UiSelector().text("Application Removed")',
    anyApplication: 'android=new UiSelector().text("Any Application")',
    okButton: 'android=new UiSelector().text("OK")',
    actionButton: 'android=new UiSelector().text("Action")',
    loggingOption: 'android=new UiSelector().text("Logging")',
    clearLog: 'android=new UiSelector().text("Clear Log")',
    systemLog: 'android=new UiSelector().text("System Log")'
};